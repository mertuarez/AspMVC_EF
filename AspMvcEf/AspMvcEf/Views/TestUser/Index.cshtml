@model ICollection<AspMvcEf.Models.Test_User>
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Layout.cshtml";
    
}

<h2>Test Typy</h2>
@Html.ActionLink("Add", "AddEdit", null, new { id = 0 }, null)

<table >
    <tr>
        <th>-</th>
        <th>-</th>
        <th>Id</th>
        <th>Url</th>
        <th>Name</th>
        <th>Username</th>
        <th>Password</th>
    </tr>
    @foreach (AspMvcEf.Models.Test_User item in Model)
    {
        <tr>
            <td>@Html.ActionLink("edit", "AddEdit", null, new { id = item.Id}, null)</td>
            <td>@Html.ActionLink("del", "Remove", null, new { id = item.Id }, null)</td>
            <td>@item.Id</td>
            <td>@item.Test_Url.Name</td>
            <td>@item.Name</td>
            <td>@item.Username</td>
            <td>@item.Password</td>
        </tr>
    }
</table>